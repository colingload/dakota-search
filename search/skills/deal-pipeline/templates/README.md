# deal-pipeline / templates

**TODO (build session):** `dashboard.template.html` — Dakota-branded pipeline dashboard snapshot.

Model on `business-strategy/playbooks/gtm-audit/templates/audit_report.html` (token + repeating-card
pattern). Reuse the Dakota palette/branding from
`holdco/dakota-search/skills/acquisition-analysis/assets/financials-side-by-side.template.html`.
Per-deal card tokens: name, phase (colored badge), fit score, SDE, next action, last contact, links.
