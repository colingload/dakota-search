---
description: Refresh the pipeline and push it to the dashboard Sheet (--export for HTML)
argument-hint: "(--export)"
---

Use the **deal-pipeline** skill to refresh the dashboard. $ARGUMENTS

Regenerate `holdco/deal-flow/pipeline.md` from the deal folders and push each row to the Google
Sheet dashboard. If `--export` is passed, also render a Dakota-branded
`holdco/deal-flow/dashboard.html` snapshot.
